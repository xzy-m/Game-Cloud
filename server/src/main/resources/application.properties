spring.application.name=server
server.port=8761

#自己注册自己，没必要
eureka.client.register-with-eureka=false
#好像是从自己获得注册信息，反正也没必要
eureka.client.fetch-registry=false
#其他客户端连接到eureka的配置，好像是
eureka.client.serviceUrl.defaultZone=http://localhost:${server.port}/eureka